import java.io.File;
import java.io.FileNotFoundException;
import java.util.Scanner;

public class scratch {
/*
    private String fileName;
    private int tokenCount;
    private String[] wordsDB;

    public void processFile(String author, String fname) throws FileNotFoundException {
        this.fileName = fname;
        //init input file
        Scanner inputFile = new Scanner(new File(fileName));

        //counts the number of tokens in the file
        if (inputFile != null) {
            while (inputFile.hasNextLine()) {
                this.tokenCount++;
                inputFile.next();
            }
        }

        //Stores the tokens in an the array wordsDB
        this.wordsDB = new String[this.tokenCount]; //init array with index == amount of words ^
        int index = 0;
        String token = null;

        while (inputFile.hasNextLine()) {
            inputFile.nextLine();
        }
    }

    ////////

    public void computeFrequency(String targetWord) {
        String targetLower = targetWord.toLowerCase();
        int frequency = 0;
        boolean found = false;
        for (String findWord : this.wordsDB) {
            try {
                String findLower = findWord.toLowerCase();
                if (findLower.equals(targetLower)){
                    frequency++;
                }
            }
            catch (Exception ignored) {}

        }
        System.out.println(frequency);
    }

*/
}
